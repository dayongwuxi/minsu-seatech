-- =====================================================================
-- V8 迁移: 注册手机号国际化 (2026-07-24)
-- 手机号不再限制中国大陆 11 位制式（支持日本 080 等），仅要求纯数字；
-- 新增国家区号列（前端可搜索国家下拉选择），手机号本体仍存纯数字，
-- 登录按注册时输入的号码精确匹配不受影响。存量行区号为 NULL。
-- =====================================================================
SET NAMES utf8mb4;
USE minsu;

ALTER TABLE member
  ADD COLUMN phone_country VARCHAR(8) DEFAULT NULL COMMENT '国家区号(如+81/+86)' AFTER phone;
